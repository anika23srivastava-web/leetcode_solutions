class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        j = 0
        for i in range(0, len(nums)):
            if nums[i] != 0:
                temp = nums[j]
                nums[j] = nums[i]
                nums[i] = temp
                j = j + 1
